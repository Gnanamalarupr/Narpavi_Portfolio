import { useState } from 'react'; import { Link, useNavigate } from 'react-router-dom'; import { motion } from 'framer-motion'; import { useAuth } from '../context/AuthContext';
export default function Login(){const [form,setForm]=useState({email:'',password:''}),[error,setError]=useState(''),[loading,setLoading]=useState(false);const {login}=useAuth(),navigate=useNavigate();const submit=async(e)=>{e.preventDefault();setLoading(true);setError('');try{const user=await login(form);navigate(user.role==='admin'?'/admin':'/')}catch(e){setError(e.response?.data?.message||'Unable to sign in.')}finally{setLoading(false)}};return <motion.div initial={{opacity:0}} animate={{opacity:1}} className="section grid min-h-[65vh] place-items-center"><form onSubmit={submit} className="card w-full max-w-md p-8"><p className="eyebrow">Maison Rosé account</p><h1 className="display mt-3 text-4xl">Welcome back.</h1><p className="mt-3 text-sm leading-6 text-[#786869]">Sign in to access your role-based experience.</p><input required type="email" className="field mt-7" placeholder="Email address" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input required type="password" className="field mt-3" placeholder="Password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><button disabled={loading} className="btn-primary mt-5 w-full">{loading?'Signing in…':'Sign in'}</button>{error&&<p className="mt-3 text-center text-sm text-rose">{error}</p>}<div className="mt-7 rounded-xl bg-blush/60 p-4 text-xs leading-6 text-[#786869]"><b>Demo access</b><br/>Admin: admin@maisonrose.in · MaisonRose@2026<br/>Customer: customer@maisonrose.in · Customer@2026</div><Link to="/" className="mt-5 block text-center text-sm text-rose">Continue as a guest</Link></form></motion.div>}
