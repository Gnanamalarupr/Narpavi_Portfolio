import {motion} from 'framer-motion'; export default function SectionTitle({eyebrow,title,text,center=false}){return <motion.div initial={{opacity:0,y:20}} whileInView={{opacity:1,y:0}} viewport={{once:true}} className={`mb-12 ${center?'text-center mx-auto max-w-2xl':''}`}><p className="eyebrow mb-3">{eyebrow}</p><h2 className="display text-4xl leading-tight md:text-5xl">{title}</h2>{text&&<p className="mt-4 leading-7 text-[#786869]">{text}</p>}</motion.div>}
